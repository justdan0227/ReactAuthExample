{
    "name": "reactauth/api",
    "description": "React Auth Example PHP API with JWT authentication",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "firebase/php-jwt": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "ReactAuth\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
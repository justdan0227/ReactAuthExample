# Used when building/deploying the Vite web app (if/when you host it).
# For local dev, prefer the Vite proxy + /api.

# If you deploy the PHP API under WordPress at:
#   https://www.iclassicnu.com/reactauth-api/api
# then set:
VITE_API_BASE_URL=https://www.iclassicnu.com/reactauth-api/api

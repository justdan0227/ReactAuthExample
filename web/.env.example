# Copy this to .env.local and adjust to your MAMP-hosted PHP API path.
#
# Examples:
# Use the Vite dev proxy (recommended for local dev; avoids CORS):
# VITE_API_BASE_URL=/api
#
# Or call MAMP directly (requires correct CORS headers on the PHP API):
# VITE_API_BASE_URL=http://localhost:8888/reactauth-api/api
#
VITE_API_BASE_URL=/api
